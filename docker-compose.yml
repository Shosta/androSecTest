version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: ./DockerFiles/Ubuntu/Dockerfile
    image: androsectest-ubuntu
  app-alpine:
    build:
      context: .
      dockerfile: ./DockerFiles/Alpine/Dockerfile
    image: androsectest-alpine